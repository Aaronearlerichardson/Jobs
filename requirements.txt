requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Free Google alternative for the web-search fetcher.
# DDG text search, no API key.
ddgs>=5.0.0

# Optional - only needed for `discover.py --capture-session` and friends.
# After install, also run: `playwright install chromium`
playwright>=1.44.0
playwright-stealth>=1.0.6
