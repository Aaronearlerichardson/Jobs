requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Local web UI (python webapp.py -> http://127.0.0.1:5533)
flask>=3.0.0
# Comment-preserving TOML writer for the Settings tab (profile.toml editing).
tomlkit>=0.12

# Free Google alternative for the web-search fetcher.
# DDG text search, no API key.
ddgs>=5.0.0

# Optional - only needed for `discover.py --capture-session` and friends.
# After install, also run: `playwright install chromium`
playwright>=1.44.0
playwright-stealth>=1.0.6

# Optional, Windows-only, NOT installed by default - desktop toast when a
# digest is emailed ([tracks.*].notify = true). Absent, the toast is
# silently skipped. Install by hand if you want it:
#   winotify>=1.1.0
