# Development / CI extras (the app itself needs only requirements.txt).
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
pyflakes>=3.0
# Standalone build (build_app.py); only needed when producing a binary.
# >=4.2: build_app.py dropped --include-package-data=playwright and now
# relies on Nuitka's own bundled playwright package config to ship the
# driver (2026-09-11); <5 because that config is not an API and a major
# bump could move it.
nuitka>=4.2,<5
zstandard>=0.22.0
